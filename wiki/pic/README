This folders contains images used for wiki pages. The larger screenshots are
put in the "large" folder, and the thumbnails of those screenshots as well as
the smaller screenshots are in the current folder "/wiki/pic". The thumbnails
are generated by the following command (using the program convert):

cd large;
for i in *.png ; do convert $i -auto-orient -thumbnail 500x260 -unsharp 0x.5  "../$i" ; done
